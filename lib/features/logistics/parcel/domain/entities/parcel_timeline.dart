class ParcelTimeline {
  final String title;
  final DateTime? time;
  final bool completed;

  const ParcelTimeline({
    required this.title,
    required this.time,
    required this.completed,
  });
}
